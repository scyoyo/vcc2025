# Core dependencies (minimal)
torch>=2.0.0

# Data handling
h5py>=3.8.0

# Progress bars (for data download)
tqdm>=4.64.0

# HTTP requests (for downloading data)
requests>=2.28.0

# Note: STATE framework will install hydra-core and other dependencies
# If you encounter "No module named 'hydra'" error, install STATE:
#   cd state && pip install -e .
